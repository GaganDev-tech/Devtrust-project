# Devtrust-project

Archeitecture :- git -> githubactions-> image built -> ECR or Dockerhub-> deployment through EC2 instances
RUN locally using docker-compose up --build
Requriements -> .env file & terraform.tfvars
